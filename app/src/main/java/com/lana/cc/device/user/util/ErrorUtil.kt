package com.lana.cc.device.user.util



/*fun Throwable.isUnableError(): Boolean =
    this is ApiException && errorModel.msgCode == "cmn.passwd.unable"

fun Throwable.isCompulsionError(): Boolean =
    this is ApiException && errorModel.msgCode == "cmn.login.userover.compulsion"*/

/*fun Throwable.isUnusableError(): Boolean {
    if (this is ApiException) {
        val msgCode = errorModel.msgCode
        msgCode.run {
            if (true && this.endsWith("unusable"))
                return true
        }
    }
    return false
}*/

