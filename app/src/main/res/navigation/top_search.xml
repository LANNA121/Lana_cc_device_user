<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_search"
    app:startDestination="@id/SearchFragment">
    <fragment
        android:id="@+id/SearchFragment"
        android:name="com.lana.cc.device.user.ui.fragment.search.SearchFragment"
        android:label="fragment_search"
        tools:layout="@layout/fragment_search">

        <action
            android:id="@+id/action_SearchFragment_to_cameraSearchFragment"
            app:destination="@id/cameraSearchFragment" />
    </fragment>
    <fragment
        android:id="@+id/cameraSearchFragment"
        android:name="com.lana.cc.device.user.ui.fragment.search.cameraSearch.CameraSearchFragment"
        android:label="CameraSearchFragment" />

</navigation>